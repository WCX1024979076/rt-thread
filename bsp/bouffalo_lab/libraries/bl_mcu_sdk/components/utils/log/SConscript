import os
from building import *

cwd = GetCurrentDir()

src = Split('''
    log.c
''')

path = [cwd]

libpath = []
libs = []

group = DefineGroup('Components_log', src, depend = [''], CPPPATH = path, LIBS = libs, LIBPATH = libpath)

Return('group')